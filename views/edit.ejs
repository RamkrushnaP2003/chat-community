<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>edit your chat</title>
    <link
      rel="stylesheet"
      href="	https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    />
    <style>
      body {
        margin: 1rem;
      }
    </style>
  </head>
  <body>
    <p>Edit your chat: <b><%= chat._id %></b></p>
    <p>This chat send was <b><%= chat.from %></b> to <b><%= chat.to %></b></p>
    <form method="POST" action="/chats/<%= chat._id %>?_method=PUT">
      <textarea class="form-control" rows="5" cols="50" name="msg"><%= chat.msg %></textarea>
      <br /><br />
      <button style="padding: 2px 10px" class="btn btn-primary">Edit</button>
    </form>
  </body>
</html>
